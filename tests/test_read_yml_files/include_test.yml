models:
  - name: test_model
    columns_mapping:
      direct_paths:
        path1: alpha.beta
        path2: alpha.beta.0.gamma
      iterate:
        path: alpha.gamma
        columns:
          path3: delta.epsilon
          path4: delta.zeta
